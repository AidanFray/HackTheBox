10.10.10.87

# Editing of /etc/host file can allow mapping of IP's to NAMEs
# tcpdump for example will resolve IPs to names using the /etc/host file


# If there is a 'wildcard response' from gobuster. We need to execude the default response
# from the status codes

# You can see the default using:

>>> curl -v <ip>/fileExtension

# Correct status codes for gobuster can be specified using "-s"

# Cool tool called "jq" for text formatting@

# .dockerenv files tell us that we're in a docker environment

# Netstat can be used to show connections in and out of a machine